export const transfers = state => state.transfers;

export const getBestPath = state => state.transfers.bestPath;

export const getAmount = state => state.transfers.amount;

export const getAsset = state => state.transfers.asset;
