import updateSource from 'immutability-helper';
import { createActions, createReducer } from 'redux-yo';

export const {{ camelCase name }}Actions = createActions(['dummy'], '{{ camelCase name }}');

const initialState = {
  dummy: undefined,
};

export const {{ camelCase name }}Reducer = createReducer(
  {
    [{{ camelCase name }}Actions.dummy]: (state, value) =>
      updateSource(state, {
        $merge: value,
      }),
  },
  initialState
);
